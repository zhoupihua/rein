---
description: Make a lightweight change — ≤5 lines, no logic impact, direct edit and commit
---

L1 lightweight change (≤5 lines, no logic impact).

Direct edit → confirm tests pass → commit.

## Instructions

1. Make the change directly — no planning, no spec, no TDD cycle
2. Run relevant tests to confirm nothing breaks
3. If tests fail, fix the issue or escalate to `/fix`
4. Commit with descriptive message

**If the change turns out to be larger than expected, stop and use `/fix` or `/feature` instead.**